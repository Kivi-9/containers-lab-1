apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-app-deployment
  labels:
    app: go-app
    lab: lab2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: go-app
  # Стратегия обновления
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  # Шаблон пода
  template:
    metadata:
      labels:
        app: go-app
    spec:
      # Использование секрета для доступа к GHCR
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: go-app
        image: ghcr.io/kivi-9/containers-lab-1/app:sha-4cea3f8 #тут должна быть ссылка на ващ образ с бэкендом на golang!
        ports:
        - containerPort: 8080
        env:
        - name: DB_HOST
          value: "postgres-service" # будем использовать имя сервиса
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "secret"
        - name: DB_NAME
          value: "myapp"
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        # Проверки готовности и жизнеспособности
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
